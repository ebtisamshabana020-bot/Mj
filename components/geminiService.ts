/**
 * Local image edit stub (no environment variables / API dependency).
 */
export const editImageWithGemini = async (
  base64Image: string,
  _mimeType: string,
  _prompt: string
): Promise<string | null> => {
  return base64Image;
};
